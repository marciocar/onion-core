# Catraca de AUFHEBUNG DE RODADA DE RADAR — rodadas seladas toleradas SEM nenhuma
# aresta SUPERSEDES e sem `meta.supersedes_none`/`supersedes_external` declarado.
# CHAVEADO (uma linha por rodada), como os 11 baselines irmãos: inteiro nu não diz QUAL
# rodada está tolerada, e some no diff quando uma sai e outra entra.
# Regenere: bash .claude/validation/radar-aufhebung-check.sh . --emit-baseline
#
# 2026-09-23 — a 1a versao deste arquivo era o inteiro `2`, depois `1`, e a passada adversarial
#   mostrou que aquele numero media 1/6 da divida: a guarda varria so os `kg:` das baselines, ou
#   seja a rodada ATUAL de cada eixo, e cada rodada nova DESPEJAVA a anterior da conta sem
#   reconciliar nada. "A metrica de saude e este numero diminuindo" era falso — o denominador era o
#   numero de eixos. Com o universo corrigido (convenção `radar-*/` UNIAO os ponteiros), o passivo
#   REAL aparece: 5. O numero subiu porque a medicao ficou honesta, nao porque a divida cresceu.
#   A `radar-E3-2026-09-21-r5` NAO esta aqui: ela declarou `meta.supersedes_none:` com a razao.
#
