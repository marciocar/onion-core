---
name: selftest-fixture-probe
description: Fixture de auto-teste do lint — agente exemplar com tools nativas válidas (Regra 12). Não deve gerar violação.
category: development
tools: [Read, Grep]
---

# Fixture de auto-teste — caso GOOD (Regra 12)

Corpo mínimo e limpo. Não referencia provider direto nem métodos de abstração,
de modo que a única regra exercitada é a Regra 12 (nomes de tool válidos).
