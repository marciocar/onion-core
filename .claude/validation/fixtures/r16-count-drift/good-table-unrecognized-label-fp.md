# Fixture de drift — caso GOOD (Regra 16, guarda anti-FP: rótulo de tabela NÃO reconhecido)

| Componente | Quantidade | Descrição |
|------------|------------|-----------|
| Total geral | __ONION_SKILLS_DRIFT__ | número deliberadamente errado |

A forma tabular só reconhece os 4 rótulos canônicos (Comandos [invocáveis], Agentes
[especializados], Skills, Knowledge Bases). 'Total geral' não é um deles — mesmo com
o número divergindo da SSOT, a Regra 16 NÃO deve citar este arquivo (rótulo fora da
allowlist deliberada, para não colidir com qualquer tabela 'rótulo | número' do repo).
