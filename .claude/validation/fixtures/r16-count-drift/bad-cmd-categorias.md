# Fixture de drift — caso BAD (Regra 16, cobertura AMPLIADA: padrão A)

Este documento afirma __ONION_COMMANDS_DRIFT__ comandos especializados organizados em 9 categorias.
A frase-de-total tem qualificadores entre 'comandos' e 'em N categorias' — antes da
ampliação ela escapava ao regex estreito. A Regra 16 (SOFT) deve flagrar a divergência.
O placeholder é substituído por (SSOT + offset) no self-test.
