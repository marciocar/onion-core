# Fixture de drift — caso BAD (Regra 16, formato BARE 'N agentes IA' c/ markdown bold)

- **__ONION_AGENTS_DRIFT__ agentes** IA distribuídos em categorias

A forma '**N agentes** IA' (bold do markdown entre 'agentes' e 'IA') é total-atual e
diverge da SSOT. O marcador ' IA' a qualifica. A Regra 16 (SOFT) deve citar este arquivo.
