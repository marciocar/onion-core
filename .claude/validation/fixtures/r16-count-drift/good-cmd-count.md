<!-- O número abaixo é o placeholder __ONION_COMMANDS_TOTAL__, substituído pela SSOT
     real (inventory.sh) em tempo de self-test (lint-selftest.sh, modo lint). Assim
     esta fixture GOOD acompanha a contagem sozinha — sem manutenção a cada comando. -->
# Fixture de drift de contagem — caso GOOD (Regra 16)

Este documento afirma __ONION_COMMANDS_TOTAL__ comandos invocáveis, em sincronia com a SSOT do
filesystem. A Regra 16 não deve flagrar este arquivo.
