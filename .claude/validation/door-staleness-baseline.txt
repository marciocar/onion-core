# Catraca de DEFASAGEM DE PORTA — quantos commits da superfície que viaja cada porta
# tolera estar atrás. SÓ ENCOLHE: a métrica de saúde é o número DIMINUINDO.
# Regenere com: bash .claude/validation/door-staleness-check.sh --emit-baseline
#
# onion-standalone 372 — o PASSIVO MEDIDO, e a razão desta guarda existir. A porta está parada
#   desde 2026-07-19 porque o ciclo de re-materialização era PROSA. Não é negligência: é o modo de
#   falha previsível de um gatilho que depende de alguém lembrar.
# onion-core 0 — porta NOVA nasce sem passivo. Se subir sem re-materialização, a guarda reprova.
#
# ⚠️ POR QUE OS NÚMEROS CAÍRAM (378/2 → 372/0) SEM NINGUÉM RE-MATERIALIZAR NADA: os tetos antigos
#   mediam `pin..HEAD` da BRANCH, então carregavam commits em voo que a porta não teria como
#   espelhar — a guarda cobrava por trabalho que ainda não existia em main. Corrigida a ponta para
#   o `merge-base`, o passivo REAL apareceu, e é menor. A catraca não afrouxou: ficou honesta.
#
# 2026-09-17, pós-merge do #840: a `main` ANDOU de verdade (4 commits na superfície que viaja), e
# os números subiram 372→373 e 0→1. Isto NÃO é a esteira curada acima: ali o teto crescia por
# trabalho em VOO, que a porta não teria como espelhar; aqui o trabalho está MERGEADO, a porta de
# fato ficou para trás, e a catraca está fazendo exatamente o que existe para fazer. O `onion-core`
# volta a 0 na re-materialização logo a seguir; o 373 do `onion-standalone` é o passivo real.
#
# 2026-09-17, pós-re-materialização: `onion-core` volta a ZERO — a porta foi refeita do `main`
# mergeado (pin 79456b1e21de) e espelha o core exatamente. É a catraca funcionando no sentido
# que interessa: o número DIMINUINDO. O `onion-standalone` sobe para 374 porque a leva do #842
# entrou em main e aquela porta segue parada desde 2026-07-19 — passivo real, não ruído.
#
# 2026-09-17 (4ª materialização): `onion-core` segue em ZERO — re-materializado do pin f5ec0dcf2dfb,
# que é a 1ª versão da porta a passar no PRÓPRIO lint (0 HARD), depois de a dívida da REGRA 45
# chegar a zero. `onion-standalone` sobe a 381: a main andou 7 commits e aquela porta segue parada
# desde 2026-07-19 — passivo real, e o único número desta catraca que ainda cresce.
#
# 2026-09-18: a catraca parou de contar o PRÓPRIO livro-caixa, e os números caíram sem ninguém
# materializar nada — 383→381 e 2→1. O que sumiu eram commits que só ajustavam ESTE arquivo: o
# teto morava na superfície que ele julga, então reconciliar a porta defasava a porta e um teto 0
# exigia um PR depois de todo PR. O `onion-core 1` que sobra é HONESTO: é o
# `regen-core-projections.sh`, mudança real de framework que a porta ainda não recebeu.
#
# 2026-09-18 (pós-#845): 383 e 3 — a main andou com o merge do #845 e com a cura dos nomes de
# cliente. Os dois sobem porque são mudança REAL de framework; o livro-caixa já não conta contra
# si mesmo (ver acima). `onion-core` volta a ZERO na próxima materialização, que é obrigatória:
# é ela que tira os nomes de cliente do HEAD público.
#
# 2026-09-18 (pós-#846): 384 e 4. A `main` andou com o merge do #846 — a cura que tirou os nomes
# de cliente das FIXTURES que viajam. Os dois sobem porque é mudança REAL de framework, medida
# contra o `merge-base`, então trabalho em voo NÃO inflou estes números.
#
# 2026-09-18 (5ª materialização, no mesmo dia): `onion-core` volta a ZERO — e o caminho que a
# tornou possível é o registro que interessa. A dificuldade era real: `materialize-door.sh` monta
# com `git archive HEAD`, e o HEAD da branch em curso tem trabalho NÃO-MERGEADO, então materializar
# de lá publicaria o que ainda não passou pelo gate. A saída NÃO foi esperar o merge: foi uma
# WORKTREE DESTACADA em `origin/main`, de onde o `git archive HEAD` é exatamente a main mergeada.
# Sequência deixa de ser motivo para adiar item de segurança.
# ⚠️ E O PIN É METADE DA CURA. Materializar sem avançar `onion_version` no `members.yaml` deixa a
#   catraca medindo contra a materialização ANTERIOR: medido aqui, a porta já estava publicada e o
#   número seguia em 4/4. O pin é a única coisa que esta guarda lê; porta nova com pin velho é
#   trabalho feito que o gate não enxerga.
# VERIFICADO NO REMOTO, não no disco: o arquivo baixado de `repos/marciocar/onion-core` traz 0 dos
# quatro termos declarados em `client-terms.txt`, com controle positivo (9 ocorrências de `PoC` no
# mesmo arquivo, provando que ele veio inteiro). Isto remove do HEAD — NÃO da história.
#
# 2026-09-18 (6ª materialização, pós-#847): `onion-core` volta a ZERO pela segunda vez no mesmo dia,
# e desta vez sem worktree — a árvore estava em `main` limpa, então `git archive HEAD` já era a main
# mergeada. O ciclo materializar→avançar-o-pin funcionou como desenhado.
#
# ⚠️ E O `onion-standalone` MERECE SER DITO EM VOZ ALTA: 384 → 388. Ele só SOBE, e sobe desde
#   2026-07-19. Uma catraca cujo único movimento é para cima não é catraca — é CONTADOR, e um
#   contador não cria pressão nenhuma: ele registra a dívida e se acomoda a ela. A guarda está
#   correta (o número é honesto); o que falta é a DECISÃO sobre aquela porta, que é do maestro e não
#   desta guarda. Enquanto ela não vem, este número é a medida exata de quanto o `onion-standalone`
#   deixou de espelhar — hoje 388 commits da superfície que viaja.
#
# 2026-09-18 (7a materializacao): `onion-core` volta a ZERO pela terceira vez no dia — o ciclo
# materializar → avancar o pin ja e mecanismo, e desta vez dispensou ate a worktree (a arvore estava
# em `main` limpa). `onion-standalone` sobe a 389.
# ⚠️ E A DECISAO (A) — RE-MATERIALIZAR o standalone — REVELOU UM BLOQUEIO QUE NENHUMA MEDICAO DE
#   CUSTO TINHA VISTO: a porta VIVA carimba `role: adopted` (corte manual de 2026-07-19), mas uma
#   materializacao NOVA carimba `role: standalone` — e CINCO predicados que decidem "este repo e
#   derivado?" so conheciam `adopted|hub`. Re-materializar trocaria a identidade da porta por uma que
#   as guardas leem como FONTE. Curado nos cinco sitios, com caso de bancada que DERIVA os papeis
#   validos do `write-stamp.sh` e reprova se algum predicado nao os conhecer todos (mutante provado).
#
# 2026-09-19 (8a materializacao): `onion-core` volta a ZERO pela quarta vez em dois dias; o ciclo
# materializar → avancar o pin roda sem pensar. `onion-standalone` sobe a 391.
# ⚠️ A DECISAO (A) DO MAESTRO — re-materializar o standalone — esta DESBLOQUEADA desde o #849, e o
#   que a destravava nao era custo: era o papel `standalone` ser invisivel para seis predicados que
#   julgam "este repo e derivado?". Materializar antes disso trocaria a identidade da porta viva
#   (que carimba `role: adopted`, do corte manual de 2026-07-19) por uma que as guardas leiam como
#   FONTE. Hoje a porta materializa em 0 HARD medidos SEM isencao — provado trocando o carimbo e
#   vendo o veredito NAO mudar de forma que compre verde.
#
# 2026-09-19/20 (9a materializacao): `onion-core` volta a ZERO pela quinta vez em tres dias — o ciclo
# materializar → avancar o pin ja nao exige pensar. `onion-standalone` sobe a 393.
# ⚠️ REGISTRO DE UMA ESPERA, porque ela e a metade que nao e minha: entre a materializacao e o push
#   houve ~1h de gate VERMELHO no core, e eu NAO avancei o pin nesse intervalo. Avancar teria feito a
#   catraca dizer 0/0 sobre uma porta que ninguem podia abrir — a armadilha INVERSA da de 2026-09-18,
#   quando o pin velho escondia porta nova. As duas mentem; a diferenca e a direcao. O pin so anda
#   depois do push VERIFICADO no remoto (`gh api .../commits/main`), nunca depois do commit local.
onion-standalone 393
onion-core 0
