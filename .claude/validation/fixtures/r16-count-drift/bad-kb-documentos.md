# Fixture de drift — caso BAD (Regra 16, forma 'Knowledge Bases (N documentos)')

5. Documentação constitucional (docs/) — Meta-specs L0, Knowledge Bases (__ONION_KBS_DRIFT__ documentos estruturados para consumo por IA), Spec as Code.

A âncora 'documentos' logo após o número marca a forma canônica usada na KB de
identidade (driftou 34→87 sem nenhum feeder disparar — PR #517). Número divergente
da SSOT: a Regra 16 deve citar este arquivo.
