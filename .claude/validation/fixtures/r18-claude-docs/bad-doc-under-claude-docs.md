# Fixture — caso BAD (Regra 18)

Documentação versionada sob `.claude/docs/` é proibida (architecture.md §2: docs vivem em
`docs/`; insumos operacionais de agente vivem em `.claude/utils/`). A Regra 18 (HARD) deve
flagrar este arquivo quando injetado em `.claude/docs/`.
