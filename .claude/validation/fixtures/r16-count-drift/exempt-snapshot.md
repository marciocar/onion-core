---
status: snapshot
---
# Fixture de drift de contagem — caso EXEMPT (Regra 16)

Naquela data de referência o sistema tinha __ONION_COMMANDS_DRIFT__ comandos invocáveis. Embora o
número divirja da SSOT atual, o frontmatter `status: snapshot` isenta o arquivo:
a Regra 16 não deve flagrar snapshots datados.
